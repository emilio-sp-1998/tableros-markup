import styled from "styled-components";
import { Layout } from "antd";

const {Header, Sider} = Layout

const Sidebar = () => {
    return (
        <Layout>
            <Sidebar className='sidebar'>xddd</Sidebar>
        </Layout>
    )
}

export default Sidebar;